// Project configuration.
grunt.initConfig({
  jshint: {
    options: {
      curly: true,
      eqeqeq: true,
      eqnull: true,
      browser: true,
      globals: {
        jQuery: true
      },
    all: ['accessibleClicks.js']
    },
    grunt.loadNpmTasks('grunt-contrib-jshint');
  },
});
